server:
  port: 7001
spring:
  application:
    name: eureka-service
eureka:
  client:
    registry-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka
